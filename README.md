# Book of Ra Deluxe — Unity preview

The index.html is the unchanged Unity Ads single-file build (4,995,528 bytes).
mraid.js is a browser preview shim, used only on GitHub Pages.
Unity Ads supplies its own real MRAID SDK; upload only index.html to Unity.
The store URL is currently unset, so INSTALL does not navigate.
